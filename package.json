{
  "name": "brc-studio-site",
  "private": true,
  "scripts": { "test": "node --test tests/worker.test.cjs && playwright test", "test:ui": "playwright test --ui" },
  "devDependencies": { "@playwright/test": "1.63.0" }
}
